<%= form_for(@friendship) do |form| %>
  <% if friendship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(friendship.errors.count, "error") %> prohibited this friendship from being saved:</h2>

      <ul>
      <% friendship.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <p>
    <% form.label :user_id %> <br/>
    <% form.text_field :user_id, value: current_user.id %>
  </p>
  <p>
    <% form.label :friend_id %> <br/>
    <% form.text_field :friend_id, value: 1 %>
  </p>
  <p>
    <% form.label :confirmed %> <br/>
    <% form.text_field :comfirmed, value: false %>
  </p>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
